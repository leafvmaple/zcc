{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Clang + LLDB",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/compiler",
      "args": [ "-llvm", "test/hello.c", "-o", "hello.ll"],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "lldb",
    }
  ]
}